{
  "name": "@on-the-road/test-fixtures",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-fixtures.mjs",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc6 -p tsconfig.json",
    "unit": "vitest run ../../tests/workspace/node-test-compat.spec.mjs --root ../..",
    "test": "pnpm run unit",
    "build": "tsc6 -p tsconfig.build.json"
  }
}
